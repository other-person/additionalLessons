import React from 'react';

type titleForFooterPropsType = {
    titleForFooter: string
}
export const Footer = (props: titleForFooterPropsType) => {
    return (

        <div>
            Footer
        </div>
    )

}
